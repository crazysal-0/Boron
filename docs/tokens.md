# Boron Tokens
This file defines how source code is broken into tokens by the lexer.

## Token Types
Every token has a type and optional value.

**_EXIT** -- Lets you output an exit code based on a **INTEGER** token after it \
**INTEGER** -- A non decimal number, has a value and that value is an int \
**NEWLINE** -- Pretty self explanatory